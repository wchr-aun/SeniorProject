1. Redux Store Update แล้ว View ที่ใช้ Store นั้นจะอัพเดทก็ต่อเมื่อ ตอน Return ในหน้า Reducer นั้น Return ด้วย Clone, Copy เท่านั้น
    - Clone Object instance -> var clone = Object.assign(Object.create(original), original);

2. ปัญหาที SellerItems กับ SellerItemsForSell มันลิ้งค์กันแม้จะใช้วิธีตามข้อ 1 แก้โดย เอา Resut มา New Waste แยก sellerItems กับ sellerItemsForSell ในกรณีที่เป็นคนละตัวแปร แต่ถ้าจะใช้ในการทำให้ Redux store update จากการ copy ก็ใช่้วิธีข้อ 1
3. จากข้อ 2 การใช้ object assign, create, prototype... ในการ Copy object นั้น ไม่ใช่ Deep Copy 